<div class="titulo">Operadores Lógicos</div>

<?php

echo "<p> V ou F </p><hr>";

var_dump(true && true); // Operador E (AND)
var_dump(true and true); // Operador E (AND)
var_dump(true || true); // Operador ou (or)
var_dump(true or true); // Operador ou (or)
var_dump(true xor true); // Operador XOR ou exclusivo
